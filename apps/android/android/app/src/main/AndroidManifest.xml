<?xml version="1.0" encoding="utf-8"?>
<!--
    The manifest overrides this app applies on top of the one `npx cap add android` generates.

    Two rules govern what may appear here:

    1. Nothing is declared for code that does not exist. A <service> or <receiver> naming a class
       with no implementation fails to resolve at runtime, and the alarm layer it would belong to
       is not written yet (see ../../../../README.md and docs/android-client-plan.md).
    2. No restricted permission is requested ahead of the feature that needs it.
       USE_EXACT_ALARM, SCHEDULE_EXACT_ALARM and USE_FULL_SCREEN_INTENT are Play-reviewed: they
       require a Console declaration and a demo video, and requesting them for functionality the
       build does not have is how a submission gets rejected rather than approved.

    `package=` is deliberately absent — Android Gradle Plugin 8 removed it in favour of
    `namespace` in build.gradle, and leaving it here fails the build.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
