{
  "name": "@medguard/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply medguard --local",
    "db:migrate:remote": "wrangler d1 migrations apply medguard --remote"
  },
  "dependencies": {
    "@hebcal/core": "^6.9.2",
    "@medguard/shared": "*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.0",
    "wrangler": "^4.118.0"
  }
}
